<view class="page">
	<map class="map" longitude="{{location.longitude}}" latitude="{{location.latitude}}" markers="{{markers}}" controls="{{controls}}" circles="{{circles}}">
	<cover-view class="cv" bindtap="find">- 智能搜索 -</cover-view>
	</map>

</view>